#!/usr/bin/bash
# ~/.config/bspwm/panel/example

#General stuff

PANEL_FIFO=/tmp/panel-fifo
. ~/.config/bspwm/panel/panel_colors

if [ $(pgrep -cx panel) -gt 1 ] ; then
	printf "%s\n" "The panel is already running." >&2
	exit 1
fi

trap 'trap - TERM; kill 0' INT TERM QUIT EXIT

[ -e "$PANEL_FIFO" ] && rm "$PANEL_FIFO"
mkfifo "$PANEL_FIFO"

#Content

bspc control --subscribe > "$PANEL_FIFO" &
xtitle -s -t 100 -f 'T%s' > "$PANEL_FIFO" &

~/.config/bspwm/panel/info.sh > "$PANEL_FIFO" &

cat "$PANEL_FIFO" | ~/.config/bspwm/panel/example_bar | lemonbar -g 1920x${PANEL_HEIGHT} -f "Source Code Pro-12" -f "FontAwesome-13" -B ${COLOR_BACKGROUND} -F ${COLOR_FOREGROUND} -u 5 | sh &

wait
